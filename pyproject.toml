[project]
name = "vietnamica-alignment"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "fonttools==4.65.0",
    "pillow>=12.3.0",
    "pymupdf==1.28.2",
    "gradio>=5.0,<7",
]

[project.optional-dependencies]
# Kept optional: the rest of the repository does not require a heavy ML stack.
character-detection = [
    "torch>=2.6",
    "torchvision>=0.21",
    "opencv-python-headless>=4.10",
    "shapely>=2.0",
]
