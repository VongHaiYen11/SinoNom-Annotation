# AutoHDR Stage 1 OCR executable

Place the extracted OCR runtime files here:

```text
dists/
└── det_model/det_model
```

Ensure the executable has execute permission on Linux/macOS and is compatible
with the host platform.
